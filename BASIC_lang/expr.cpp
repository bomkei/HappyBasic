#include "main.h"

Object RunExpr(Node* node)
{
  if( node == nullptr )
    return { };



  return { };
}

Object RunStmt(Node* node)
{
  if( node == nullptr )
    return { };

  switch( node->type )
  {

  }

  return { };
}